# OPP backend — copy to .env for local development (docker compose postgres)
PORT=3100
PCP_DATABASE_URL=postgresql://opp:opp_dev_password@localhost:5432/opp

# Optional: read production data from HAE PostgreSQL (registers hae.postgres adapter)
# ALLOCATION_DATABASE_URL=postgresql://user:pass@localhost:5432/allocation
