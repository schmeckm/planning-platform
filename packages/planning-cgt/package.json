{
  "name": "@PCP/planning-cgt",
  "version": "0.1.0",
  "description": "Pharma Collective Platform – Cell & Gene Therapy industry pack",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm -r --filter \"@PCP/planning-cgt...\" build",
    "test": "vitest run"
  },
  "dependencies": {
    "@PCP/planning-core": "workspace:*",
    "@PCP/planning-constraints": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
